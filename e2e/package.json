{
  "name": "pac-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Pruebas E2E de sistema (Playwright) — Fase 11",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "otplib": "^12.0.1"
  }
}
