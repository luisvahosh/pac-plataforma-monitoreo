{
  "name": "pac-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Worker/scheduler — andamiaje Fase 2 (sin lógica de notificaciones aún)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.6.0"
  }
}
